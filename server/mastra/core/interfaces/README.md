# `server/mastra/core/interfaces/`

Interfaces and contracts for Mastra agent components (memory, history, etc.).

## Why this matters

Interfaces here keep agent logic decoupled from storage/providers, making it easier to swap implementations without rewriting the agent.


