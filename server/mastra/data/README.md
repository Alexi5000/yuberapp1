# `server/mastra/data/`

Agent-related seed/mock data used during development.

## Notes

If you see “mock” files here, treat them as local/dev fixtures only. Production flows should use real APIs + database sources.


