# `server/routers/`

tRPC router modules split out from the main `server/routers.ts`.

## What lives here

- `agent.ts`: agent-related endpoints used by Help/Support and Settings screens

# `server/routers/`

tRPC sub-routers mounted under the main `appRouter` (`server/routers.ts`).

## What lives here

- `agent.ts`: agent-related endpoints (help/settings).


