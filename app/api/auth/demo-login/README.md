# `app/api/auth/demo-login/`

Demo-only route handler that issues a session without real credentials.

## Warning

Do not enable in production unless you have strong gating (env flag, allowlist, etc.).


