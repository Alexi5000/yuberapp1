# `patches/`

Patch files applied to third-party dependencies when upstream fixes aren’t available or when temporary workarounds are required.

## Notes

If you add new patches, document why they exist and what upstream issue they correspond to.